# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env (절대 커밋 금지 — DB 비밀번호·API 키 포함)
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# prisma
/prisma/*.db

# 분석·설계 작업 산출물 (커밋 제외)
.understand-anything/
.superpowers/
docs/superpowers/

# 자체 서버 배포 런타임 기록 (서버에서 생성)
.deployed-sha

# Claude Code 로컬 설정 (preview 실행 구성 등)
.claude/

# Agent skill config (per-machine, not shared)
CLAUDE.md
docs/agents/

# 교수님이 SFTP 로 올리는 정적 자료. 저장소에 올리지 않고 서버에만 둔다.
# Caddy 가 aicms.pusan.ac.kr/aice 로 직접 서빙한다(앱을 거치지 않음).
# 주의: ~/cms 안에 있으므로 이 폴더가 있는 상태에서 `git clean -fdx` 를 절대 실행하지 말 것.
#       -x 는 gitignore 된 파일까지 지운다.
/aice/
